# jedisec-bootstrap
JediSec Bootstrap - Modular, category-driven environment manager for Termux / Linux / WSL2. Installs system + Python packages by category, health checks, dry-run, JSON summaries, config overrides.
